export const CHECKOUT_CONFIG = Object.freeze({
    freeShippingThreshold: 50,
    shippingFee: 9.99,
    vatRate: 0.12, // 12% VAT (database prices are VAT-inclusive)
});

export const UI_CONFIG = Object.freeze({
    searchDebounceMs: 200,
});
